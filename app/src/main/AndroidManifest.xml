<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.msi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/islam"
        android:label="Allohni 99ta ismini bilasizmi"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
    <activity
        android:label="Allohni 99ta ismini bilasizmi"
        android:name=".LotinActivity">
    </activity>
        <activity
            android:label="Аллохни 99та исмини биласизми"
            android:name=".KirilActivity">
        </activity>
        <activity
            android:label="Eslatma"
            android:name=".Reminder">

        </activity>
    </application>
</manifest>
